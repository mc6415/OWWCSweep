extends ./layout.pug

block content
  table.table
    thead
      tr
        th Country
        th Taken By
    tbody
      for c in countries
        tr
          td=c.name
          td=c.takenBy
          td
            a.btn.btn-info(href='/country/view/' + c.code) View More
